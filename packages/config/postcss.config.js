/**
 * LynxKit 共享 PostCSS 配置
 *
 * 用法（在 app 中引用）：
 *   import postcssConfig from '@lynxkit/config/postcss';
 *   export default postcssConfig;
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
